# Test dependencies for Alexa OAuth2 Integration
# Install with: pip install -r requirements-test.txt

# Home Assistant core - upgraded for Python 3.12 compatibility
homeassistant==2024.1.0

# Core testing - all pinned for compatibility
pytest==7.3.1
pytest-cov==3.0.0
pytest-asyncio==0.20.3
pytest-homeassistant-custom-component==0.13.42
coverage==7.2.7

# Required production dependencies (pinned for homeassistant 2024.1.0)
# Note: pyyaml is installed as transitive dependency of homeassistant
PyJWT==2.8.0
cryptography>=43.0.0

# Code quality tools
mypy>=1.5.0
flake8>=6.0.0
